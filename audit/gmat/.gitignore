# GMAT install + download cache (large, regenerable via install_gmat.sh)
.gmat-R2020a/
.cache/

# GMAT runtime artifacts
GmatLog.txt
*.fermi-bak
*.bak

# NOTE: out/ (the raw GMAT report files + the engine-vs-GMAT comparison output) is
# intentionally COMMITTED so the inputs, config and outputs are all inspectable in the
# repo without running GMAT. run_gmat.sh regenerates it.
